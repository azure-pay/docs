# VoucherPay Mintlify merchant docs

This folder is a standalone Mintlify documentation source for merchant-facing
VoucherPay docs.

Run from this directory:

```bash
npx mintlify dev
```

The config uses `docs.json`, which is Mintlify's current configuration format.
The old `mint.json` format is intentionally not used.
